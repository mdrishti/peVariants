# Shared password testers must enter to access the app.
# For local dev/build: copy this file to .env.local and set a real value (gitignored, won't be committed).
# For hosted deploys: set VITE_APP_PASSWORD in your host's environment variables dashboard instead.
VITE_APP_PASSWORD=changeme
