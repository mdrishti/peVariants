import MutationLevel from './MutationLevel';
import data from '../data/level6.json';

export default function Level6() {
  return <MutationLevel data={data} title="Level 6 — Multiple Letters at Once (Block Substitution)" levelKey="level6" />;
}
